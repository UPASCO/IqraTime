node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
*.log
.DS_Store
*.orig.*
.env
.env.local
ios/
android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
coverage/
.metro-health-check*

# Store-submission credentials. The Google Play service account JSON has no
# distinctive extension (it is plain .json), so it is pinned to this one
# directory and ignored wholesale — an accidental `git add` of it would
# hand over publish rights to the Play listing.
secrets/
